# 2025.11.27

> #### 🎈5.5h （累計時間 54.5h）

## 作業内容

- デイトラ中級課題
  - Taskモデル
    - update, destroyアクション
  - リファクタリング
  - Commentモデル


## 学び
### ネストされたモデルでの form_with & render
- **form_with** の書き方 → **modelに親と子のモデルを配列で渡す**  

例）boards/:board_id/tasks/:id  
`= form_with model: [@board, @task] do |f|`

    - new → board_tasks_path(@board)（POST）
    - edit → board_task_path(@board, @task)（PUT）

- newとeditのviewでの **render** の書き方 → **変数を二つ指定**  

例）`= render "form", board: @board, task: @task`