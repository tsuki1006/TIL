# 2025.11.25

> #### 🎈1.0h （累計時間 45.5h）

## 作業内容

- デイトラ中級課題
  - BoardモデルのshowのCSS付け（途中）

## 学び
### CSS
#### 要素全体をリンクにする
- クリック領域は一つの`<a>`にまとめる
- `<a>`は**ブロック要素化**する
- `<a>`のブロック内部に別のclickable要素を置かない → 分離すること

#### `<a>`のブロック要素化
→ `<a>`のdisplayを block / flex / grid にする

#### calc()について

#### 文字の折り返し `overflow-wrap`
`overflow-wrap` 単語が長すぎるときに単語の途中でも折り返す

- 英単語やURLが要素をはみ出す理由
  → 英語（半角英数字）は**単語単位**で扱われるから

## MEMO
3連休しっかり休めた！また切り替えて積み重ねよう