# 2025.11.19

> #### 🎈4.5h （累計時間 38.5h）

## 作業内容

- デイトラ中級課題
  - Taskモデル
    - new, createアクション
    - 日本語化

## 学び
- Newアクションでモデルの新規インスタンスを作成時、ハッシュを引数で渡せる

  例）
    ```ruby
    def new
      @task = @board.tasks.build(user: current_user)
    end
    ```

- Strong Parameterにはform内の項目以外はいれない。formの内容以外の項目は、別で紐づける。
- Create アクションで新規レコードに、formからわたってきたparams（Storng Params）以外にも項目を追加する方法

  例）
    ```ruby
    def create
      @task = @board.tasks.build(task_params)
      @task.user = current_user
      ...
    end
    ```
