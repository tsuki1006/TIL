# 2025.11.30

> #### 🎈6.0h （累計時間 63.5h）

## 作業内容

- デイトラ中級課題
  - Profileモデル
    - ユーザーアイコン
  - コメント数
  - 最終リファクタ（途中）

## 学び
### ActiveStorage使用時の注意
`has_one_attached :image`（Active Storage）使用時、`profile.image` は `ActiveStorage::Attached::One`というオブジェクトを返す（**添付されていなくても**）

#### → `attached?`を使う

### `attached?`
：ActiveStrageの添付ファイルが存在するかどうかを判定するメソッド

**ポイント**
“attachment オブジェクトがあるかどうか” ではなく “実際にファイルがストレージに保存されているかどうか” を見ている

### CSS 画像を重ねて表示する
`display: flex;` + `margin-left: -◯px`

### CSS セレクターの書きかた 子要素
    .header .dropdown {
    ...
    }


## 調べること
- 配列の操作メソッド `selest`, `reject`
- 三項演算子復習